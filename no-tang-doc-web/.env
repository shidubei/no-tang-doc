# Keycloak environment variables for Vite (must start with VITE_)
VITE_KEYCLOAK_URL=http://localhost:8080/
VITE_KEYCLOAK_REALM=ntdoc
VITE_KEYCLOAK_CLIENT_ID=no-tang-doc-core
VITE_API_BASE_URL=http://localhost:8070

# After copying to .env.local or .env, adjust the values to match your Keycloak setup.
# If using HTTPS and a reverse proxy, ensure the URL matches exactly (with trailing slash).

